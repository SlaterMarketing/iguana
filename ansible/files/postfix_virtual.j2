# Managed by ansible/mail.yml. Every address delivers LOCALLY (fleet rule: never forward instead of delivering).
{% for name in mail_inbox_aliases %}
{{ name }}@{{ site_domain }} inbox
{% endfor %}
dmarc@{{ site_domain }} dmarc
