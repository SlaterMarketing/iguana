---
import TableQrPage from "../../../../components/menu/TableQrPage.astro";
import { isTableNumber } from "../../../../lib/menu";

const { table } = Astro.params;
if (!isTableNumber(table)) return Astro.redirect("/es/menu/", 302);
---

<TableQrPage locale="es" table={Number(table)} />
