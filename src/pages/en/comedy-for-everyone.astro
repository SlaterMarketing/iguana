---
export const prerender = true;
import { getLocale } from "../../i18n/locale";
import { localizePath } from "../../i18n/routes";

const locale = getLocale(Astro);
return Astro.redirect(localizePath(locale, "about"), 301);
---
